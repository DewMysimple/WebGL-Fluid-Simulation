---
type: state
status: active
kind: process
importance: high
updated: 2026-09-25
topic: project-constraints
source_logs:
  - "[[日志/2026-09-24-现代架构与双语重构]]"
  - "[[日志/2026-09-25-自动提交与推送约定]]"
supersedes: null
---

# 当前约束 / Current constraints

> 本次基线已于 2026-09-24 获用户集中确认，当前有效。
> Baseline confirmed by the user on 2026-09-24; active.

- Node.js 24（`.nvmrc`）；npm 锁定依赖；TypeScript strict。记忆工具需要 Python 3.10+。
- 浏览器需要可渲染半浮点纹理。WebGL 2 优先，WebGL 1 回退；缺少线性过滤时关闭部分光效并手动插值。
- 浏览器可执行内容仅从 `dist/` 发布；无后端和环境变量。当前 CI 只检查，不发布。
- 中英文文本键对齐；参数和语言分别持久化；存储失败不阻止使用。
- 每次资源分配必须有清理路径，保留 MIT 许可及作者归属。
- 用户授权本次架构判断、重构、双语和记忆构建；未要求提交、推送或线上发布。
- 用户要求今后每次完整对话完成本仓库修改后，检查改动、记录日志、提交本次对话的项目改动，并推送至当前分支已配置的上游；执行前排除与本轮无关的用户文件。
- 模板原始文件保持只读；本次基线已获确认。后续新增技术结论在确认前为 proposed，日志追加不覆写。

Use Node 24 and the lockfile; keep the engine independent, GPU cleanup explicit, and both UI languages complete. At the end of each completed conversation that changes the repository, inspect, log, commit and push that conversation's intended project changes to the current upstream. Preserve unrelated files. Only verified behavior belongs in logs. No production deployment has been requested.

依据：`package.json`、`tsconfig.json`、`src/engine/webgl/context.ts`、`LICENSE`、用户本次任务、`wiki_memory/AGENTS.md`。

- [[知识/规范/工程规范]]
- [[知识/流程/开发与验证]]

## 来源 / Evidence

- [[日志/2026-09-24-现代架构与双语重构|重构工作日志]]
